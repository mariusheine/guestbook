{
  "name": "guestbook",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "NITRO_PRESET=bun nuxt build",
    "dev": "bun --bun nuxt dev",
    "generate": "nuxt generate",
    "preview": "mkdir -p .output/.data && ln -s ../migrations .output/migrations && nuxt preview",
    "postinstall": "nuxt prepare",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:ui": "bun drizzle-kit studio"
  },
  "dependencies": {
    "@iconify-json/heroicons": "^1.1.22",
    "@nuxt/fonts": "^0.7.1",
    "@nuxt/ui": "^2.17.0",
    "@vueuse/core": "^10.11.0",
    "dayjs": "^1.11.11",
    "drizzle-orm": "^0.32.0",
    "nuxt": "^3.12.3",
    "openai": "^4.52.7",
    "vue": "latest",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "bun-types": "^1.1.18",
    "drizzle-kit": "^0.23.0",
    "prettier": "^3.3.2"
  }
}
